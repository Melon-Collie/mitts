class_name SupabaseConfig

# Replace with your project's values from the Supabase dashboard (Settings → API).
# The anon key is safe to commit — RLS restricts it to INSERT/SELECT/UPDATE only.
const URL: String = "https://YOUR_PROJECT_REF.supabase.co"
const ANON_KEY: String = "YOUR_ANON_KEY"
